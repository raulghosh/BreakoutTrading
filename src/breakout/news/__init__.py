"""News & theme overlay (design doc, Section 5) — the core of the 'early' thesis. Phase 5 stubs.

- theme_graph: catalyst -> theme -> 1st/2nd-order beneficiaries (powers L5 + early discovery)
- catalyst_llm: per-candidate structured catalyst scoring (durability dominates)
- attention: news-volume / options-activity abnormality signals (lead price)
"""

from .theme_graph import ThemeGraph  # noqa: F401
